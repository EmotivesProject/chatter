CREATE TABLE messages(
   id uuid,
   username_to text,
   username_from text,
   message text,
   created timestamp,
   PRIMARY KEY ((username_to, username_from), created)
) WITH CLUSTERING ORDER BY (created DESC);

create index on messages(username_to);
create index on messages(username_from);